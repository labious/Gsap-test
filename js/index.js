var card = document.getElementsByClassName('story');

window.addEventListener("load",
function () {
    
var listItem = document.getElementsByClassName("listItem");
 },
    false);

function enlarge(objw) {
    
    objw.classList.add("scale");
    console.log(objw);
}